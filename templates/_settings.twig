{% import "_includes/forms" as forms %}


{{ forms.selectField({
    label: "Asset source"|t,
    instructions: "Where do you want to store the cropped images?"|t,
    id: 'assetSource',
    name: 'assetSource',
    options: craft.cropAssets.assetSourceOptions,
    value: settings.assetSource
})}}
